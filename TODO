
* [DONE] implement topological sort that returns groups of nodes instead of
   node-by- node

* [DONE] DAG0 -> AST converter

* [DONE] composite-gate-unroller

* ocamldot cannot be installed by opam, but can be from build-tree.
   Figure this out
